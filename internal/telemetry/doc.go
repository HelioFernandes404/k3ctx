// Package telemetry writes structured event records that the CLI emits
// during connect/disconnect/tunnel operations, plus the reader used by
// status commands to surface the most recent events.
package telemetry
