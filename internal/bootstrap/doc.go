// Package bootstrap is the composition root. It is the only package that
// instantiates infrastructure adapters and assembles them into the
// ServiceContainer consumed by the CLI.
package bootstrap
