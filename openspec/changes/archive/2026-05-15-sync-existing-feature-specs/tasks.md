## 1. Spec Baseline

- [x] 1.1 Add `local-configuration` baseline spec to main `openspec/specs/`.
- [x] 1.2 Add `inventory-discovery` baseline spec to main `openspec/specs/`.
- [x] 1.3 Add `cluster-connection` baseline spec to main `openspec/specs/`.
- [x] 1.4 Add `tunnel-management` baseline spec to main `openspec/specs/`.
- [x] 1.5 Add `k9s-launch` baseline spec to main `openspec/specs/`.

## 2. Validation

- [x] 2.1 Validate the OpenSpec change with strict validation.
- [x] 2.2 Confirm no Go implementation changes are included in this sync.
- [x] 2.3 Archive the change after validation so the baseline specs become current.
